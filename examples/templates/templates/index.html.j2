<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ site_title }}</title>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Helvetica, Arial, sans-serif; margin: 2rem; line-height: 1.5; }
      .container { max-width: 720px; margin: 0 auto; }
      h1 { color: #ba0000; }
      .card { border: 1px solid #ddd; border-radius: 8px; padding: 1rem 1.25rem; box-shadow: 0 1px 2px rgba(0,0,0,0.05); }
      .muted { color: #666; font-size: 0.9rem; }
      code { background: #f6f8fa; padding: 0.15rem 0.3rem; border-radius: 4px; }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>{{ site_title }}</h1>
      <div class="card">
        <p>{{ welcome_message }}</p>
      </div>
      <p class="muted">Generated by {{ generated_by | default('Ansible') }} on {{ ansible_date_time.date }} {{ ansible_date_time.time }}.</p>
      <p class="muted">Host: {{ inventory_hostname }}</p>
    </div>
  </body>
</html>


